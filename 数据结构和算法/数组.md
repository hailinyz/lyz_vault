
**随机访问**
根据索引查找元素，时间复杂度是O(1)

## 动态数组

能够插入、删除元素，数组长度能根据需要发生变化，就叫做**动态数组**
所谓的扩容就是创建一个新的数组

自己实现的动态数组，Java已经实现了动态数组**ArrayList**可以直接用就行了
```java
public class DynamicArrary {  
  
    private int size = 0;   //逻辑大小  
    private int capacity = 8;   //容量  
    private int[] array = new int[capacity];  
  
    /*  
    添加元素addLast()  
     */    public void addLast(int element){  
        array[size] = element;  
        size++;  
    }  
  
    /*  
    添加元素add(index,element)指定位置添加元素，然后移动后面的元素  
     */    public void add(int index, int element){  
        if (index >= 0 && index <= size){  
            System.arraycopy(array,index,array,index+1,size-index); //拷贝移动元素  
            array[index] = element;  
            size++;  
        }else if (index == size){ //addLast() 这里将两个方法合并了，覆盖了上面的addLast  
            array[size] = element;  
            size++;  
        }  
    }  
      
}
```


### 动态数组遍历
1. 函数式接口，使用Consumer<T>无需返回值， 然后调用放自行选择想要实现的功能，传参数就行了
![](assets/数组/file-20251214001334996.png)

2. 迭代器（推荐Java中常用） 其实就是增强for循环，直接用就行了，Java中的迭代器循环
实现  Iterable<T> 接口得到 iterator() 方法

3. 还有Java中流的方式进行遍历 **Stream**


### 删除







